export enum AddressHeaderTheme {
  Blue = 'Blue',
  Light = 'Light',
}

export enum FROM_STEP {
  INFO,
  REVIEW,
  ESTIMATION,
  // Нужно для общего подсчета количества, доп стэпы писать выше
  __LENGTH,
}
